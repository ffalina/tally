# tally

A tiny math expression solver written in Go. Parses and evaluates expressions like `3 + 4 * 2 / (1 - 5)^2`, with support for functions (`sqrt`, `sin`, `abs`, `log`, ...) and step-by-step output.

## Usage

```sh
go run ./src
```

## Test

```sh
go test ./src
```

## License

MIT
